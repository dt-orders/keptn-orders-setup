apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: catalog-service
  labels:
    app: {{ .Chart.Name }}-selector-catalog-service
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.catalogService.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-catalog-service
        deployment: catalog-service
    spec:
      containers:
      - name: {{ .Values.catalogService.container.name }}
        image: "{{ .Values.catalogService.image.repository }}:{{ .Values.catalogService.image.tag }}"
        imagePullPolicy: {{ .Values.catalogService.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.catalogService.image.repository }}:{{ .Values.catalogService.image.tag }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.catalogService.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}

