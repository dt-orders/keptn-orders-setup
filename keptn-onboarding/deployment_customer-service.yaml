apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: customer-service
  labels:
    app: {{ .Chart.Name }}-selector-customer-service
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.customerService.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-customer-service
        deployment: customer-service
    spec:
      containers:
      - name: {{ .Values.customerService.container.name }}
        image: "{{ .Values.customerService.image.repository }}:{{ .Values.customerService.image.tag }}"
        imagePullPolicy: {{ .Values.customerService.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.customerService.image.repository }}:{{ .Values.customerService.image.tag }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.customerService.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}
