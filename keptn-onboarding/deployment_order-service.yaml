apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: order-service
  labels:
    app: {{ .Chart.Name }}-selector-order-service
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.orderService.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-order-service
        deployment: order-service
    spec:
      containers:
      - name: {{ .Values.orderService.container.name }}
        image: "{{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}"
        imagePullPolicy: {{ .Values.orderService.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.orderService.image.repository }}:{{ .Values.orderService.image.tag }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.orderService.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}

